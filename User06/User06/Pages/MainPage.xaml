<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="User06.Pages.MainPage">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="&#10133;" Order="Primary" Clicked="NewProject_Clicked"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <ListView
            x:Name="ProjectsLV"
            ItemSelected="ProjectsLV_ItemSelected"
            ItemsSource="{Binding}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ViewCell.View>
                            <StackLayout>
                                <Label Padding="10" Text="{Binding ULR}" TextColor="Black" FontSize="Medium" />
                                <Label Padding="10" Text="{Binding TypeName}" TextColor="Black" FontSize="Medium" />
                            </StackLayout>
                        </ViewCell.View>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>